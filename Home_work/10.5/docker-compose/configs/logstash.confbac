input {
  tcp {
    port => 5044
    codec => json
    ssl => false
  }
}

filter {
  json {}
}

output {
  elasticsearch {
    hosts => ["es-hot:9200"]
    index => "logstash-%{[@metadata][indexDate]}"
  }
  stdout { codec => rubydebug }
}
